# v4.0.1 — Fix HTTP 404 on Vercel

API routing was split into explicit Vercel filesystem routes:
- /api/health
- /api/v1/auth/*
- /api/v1/extension/*
- /api/admin/*

This avoids relying on a single root catch-all function.
